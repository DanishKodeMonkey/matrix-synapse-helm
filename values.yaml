global:
  domain: danielrunge.dk

synapse:
  image:
    repository: matrixdotorg/synapse
    tag: sha-f79acff # latest non-dev build as of writing
    pullPolicy: IfNotPresent
  storage:
    size: 10Gi
    storageClassName: ""  # optional
